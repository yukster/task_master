info:
  name: Get task by id
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:4000/api/tasks/:id
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
