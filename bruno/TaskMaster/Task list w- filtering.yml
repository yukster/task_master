info:
  name: Task list w/ filtering
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:4000/api/tasks
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
